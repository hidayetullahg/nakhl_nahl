name: nakhl_nahl
description: "NAKHL & NAHL Uluslararası Muhasebe ve Cari Yönetim Sistemi"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3
  supabase_flutter: ^2.6.0
  http: ^1.2.0
  crypto: ^3.0.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true